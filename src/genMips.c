/*
 *  traduit le code 3 adresses en code mips
 * la fonction écrira le code dans le fichier de sorti directement
 */
void printMips(genCode* code)
{
    char* 
    struct code3add codeSrc = code.tab;
    while(codeSrc != NULL)
    {
    
    }
}
