// test des expressions sans structure de controle
class Program {

    int nombre;
    boolean test;

    nombre = 4;
    nombre += 2;
    nombre = nombre + nombre - nombre;

    test = nombre == 6 || nombre != 2 && nombre < 10;
}
